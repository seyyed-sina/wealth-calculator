import { ProfileForm } from '@components';

const ProfilePage =  () => {
  // const {
  //   data: { user },
  // } = await getUser();

  // if (!user) {
  //   return <div>Unauthorized</div>;
  // }

  return <ProfileForm />;
};

export default ProfilePage;
